# -------------------------------------------------------------------
# SMB FinSight - Standard-specific configuration
# Canada / ASPE
# -------------------------------------------------------------------

[paths.mapping]
# Primary income statement mapping under CA ASPE
income_statement = "../mapping/income_statement_ca_aspe.csv"

# No secondary statement at this stage (can be added later if needed)
secondary_mapping = ""

# Optional: chart of accounts reference file for CA ASPE.
# This file can be extended/customized by the user.
chart_of_accounts = "../data/reference/ca_aspe.csv"

[statements]
# Human-readable label for the primary statement (income statement).
primary_label = "Income Statement (CA ASPE)"

# No secondary statement label for now.
secondary_label = ""

[ratios]
# Main ratios rules pack for CA ASPE.
rules_file = "../ratios/ratios_ca_aspe.toml"

# Optional company-specific ratios pack (can extend or override the main pack).
#custom_rules_file = ""
