
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smb-finsight"
version = "0.5.0"
description = "SMB FinSight - Financial Dashboard & Analysis application for SMBs"
authors = [{ name = "Maxence Bernard", email = "maxence@maxencebernardhub.com" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = ["pandas>=2.0.0","numpy>=1.23.0"]

[project.optional-dependencies]
dev = [
  "pytest>=9.0.3",
  "ruff>=0.6"
]

webui = [
  "streamlit>=1.51.0",
  "tomlkit>=0.12"
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 88
target-version = "py311"
src = ["src", "tests"]
exclude = [
  ".venv",
  ".ruff_cache",
  "src/smb_finsight.egg-info",
  "src/smb_finsight/__pycache__",
]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP"]
ignore = []
fixable = ["ALL"]

[tool.ruff.format]
quote-style = "preserve"
indent-style = "space"
line-ending = "auto"
docstring-code-format = true
